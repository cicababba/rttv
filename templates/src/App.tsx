export default function App() {
    return (
        <div className="h-screen flex items-center justify-center bg-gray-50">
            <h1 className="text-3xl font-bold text-primary-500">
                Hello, React + Vite + TS + Tailwind!
            </h1>
        </div>
    )
}
